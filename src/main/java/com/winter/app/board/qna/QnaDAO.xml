<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.winter.app.board.qna.QnaDAO">

	<resultMap type="QnaVO" id="detailResultMap">
		<id column="BOARD_NUM" property="boardNum"/>
		<result column="BOARD_TITLE" property="boardTitle"/>
		<result column="BOARD_CONTENT" property="boardContent"/>
		<result column="BOARD_WRITER" property="boardWriter"/>
		<result column="BOARD_DATE" property="boardDate"/>
		<result column="BOARD_HIT" property="boardHit"/>
		<result column="BOARD_REF" property="boardRef"/>
		<result column="BOARD_STEP" property="boardStep"/>
		<result column="BOARD_DEPTH" property="boardDepth"/>
		<collection property="boardFileVO" javaType="java.util.List" ofType="BoardFileVO">
			<result column="FILE_NUM" property="fileNum"/>
			<result column="ORI_NAME" property="oriName"/>
			<result column="SAVE_NAME" property="saveName"/>
		</collection>
	</resultMap>
	
	<sql id="search">
		<where>
			<choose>
				<when test='kind == "k1"'>BOARD_TITLE</when>
				<when test='kind == "k2"'>BOARD_CONTENT</when>
				<otherwise>BOARD_WRITER</otherwise>			
			</choose>
			LIKE CONCAT('%',#{keyword},'%')
		</where>
	</sql>
	
	<select id="noticeList" resultType="QnaVO" parameterType="Pager">
		SELECT * FROM qna 
		<include refid="search"></include>
		ORDER BY BOARD_REF DESC, BOARD_STEP ASC
		LIMIT #{startIndex}, #{endIndex}
	</select>
	
	<insert id="insert" parameterType="QnaVO" useGeneratedKeys="true" keyProperty="boardNum">
		INSERT INTO qna (BOARD_NUM, BOARD_TITLE, BOARD_CONTENT, BOARD_WRITER, BOARD_DATE, BOARD_HIT, BOARD_REF, BOARD_STEP, BOARD_DEPTH) 
		VALUES (NULL, #{boardTitle}, #{boardContent}, #{boardWriter}, now(), 0, 0, 0, 0)
	</insert>
	
	<update id="refUpdate" parameterType="QnaVO">
		UPDATE qna SET BOARD_REF = #{boardNum} WHERE BOARD_NUM = #{boardNum}
	</update>
	
	<select id="detail" parameterType="QnaVO" resultMap="detailResultMap">
		SELECT * FROM qna q LEFT JOIN qna_files qf ON q.BOARD_NUM = qf.BOARD_NUM 
		WHERE q.BOARD_NUM = #{boardNum}
	</select>
	
	<update id="replyUpdate" parameterType="QnaVO">
		UPDATE qna SET BOARD_STEP = BOARD_STEP + 1
		WHERE BOARD_REF = #{boardRef} AND BOARD_STEP > #{boardStep}
	</update>
	
	<insert id="replyInsert" parameterType="QnaVO" useGeneratedKeys="true" keyProperty="boardNum">
		INSERT INTO qna (BOARD_NUM, BOARD_TITLE, BOARD_CONTENT, BOARD_WRITER, BOARD_DATE, BOARD_HIT, BOARD_REF, BOARD_STEP, BOARD_DEPTH)
		VALUES (NULL, #{boardTitle}, #{boardContent}, #{boardWriter}, now(), 0, #{boardRef}, #{boardStep}, #{boardDepth})
	</insert>
	
	<select id="getTotalCount" resultType="Long" parameterType="Pager">
		SELECT COUNT(BOARD_NUM) FROM qna <include refid="search"></include>
	</select>
	
	<insert id="insertFile" parameterType="BoardFileVO">
		INSERT INTO qna_files VALUES (NULL, #{boardNum}, #{oriName}, #{saveName})
	</insert>
	
	<delete id="fileDelete" parameterType="QnaVO">
		DELETE FROM qna_files WHERE BOARD_NUM = #{boardNum}
	</delete>
	
	<update id="delete" parameterType="QnaVO">
		UPDATE qna SET BOARD_TITLE = 'DELETED_POST_CODE_20001020' WHERE BOARD_NUM = #{boardNum}
	</update>
	
	<delete id="fileDeleteByFileNum" parameterType="BoardFileVO">
		DELETE FROM qna_files WHERE FILE_NUM = #{fileNum}
	</delete>
	
	<update id="update" parameterType="QnaVO">
		UPDATE qna SET BOARD_TITLE = #{boardTitle}, BOARD_WRITER = #{boardWriter}, BOARD_CONTENT = #{boardContent}
		WHERE BOARD_NUM = #{boardNum}
	</update>
	
	<select id="fileDetail" parameterType="BoardFileVO" resultType="BoardFileVO">
		SELECT * FROM qna_files WHERE FILE_NUM = #{fileNum}
	</select>
	
</mapper>