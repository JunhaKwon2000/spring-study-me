<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.winter.app.board.notice.NoticeDAO">

	<resultMap type="NoticeVO" id="detailResultMap">
		<id column="BOARD_NUM" property="boardNum"/>
		<result column="BOARD_TITLE" property="boardTitle"/>
		<result column="BOARD_CONTENT" property="boardContent"/>
		<result column="BOARD_WRITER" property="boardWriter"/>
		<result column="BOARD_DATE" property="boardDate"/>
		<result column="BOARD_HIT" property="boardHit"/>
		<collection property="boardFileVO" javaType="java.util.List" ofType="BoardFileVO">
			<!-- duplicate column when join(board_num) should be excluded -->
			<result column="FILE_NUM" property="fileNum"/>
			<result column="ORI_NAME" property="oriName"/>
			<result column="SAVE_NAME" property="saveName"/>
		</collection>
	</resultMap>

	<sql id="search">
		<where>
			<choose>
				<when test='kind == "k1"'>BOARD_TITLE</when>
				<when test='kind == "k2"'>BOARD_CONTENT</when>
				<otherwise>BOARD_WRITER</otherwise>			
			</choose>
			LIKE CONCAT('%',#{keyword},'%')
		</where>
	</sql>

	<!-- Query ID는 DAO의 메서드명과 동일하게 작성 -->
	<insert id="insert" parameterType="NoticeVO" useGeneratedKeys="true" keyProperty="boardNum">
		INSERT INTO notice (BOARD_NUM, BOARD_TITLE, BOARD_CONTENT, BOARD_WRITER, BOARD_DATE, BOARD_HIT) 
		VALUES (NULL, #{boardTitle}, #{boardContent}, #{boardWriter}, now(), 0)
	</insert>
	
	<update id="update" parameterType="NoticeVO">
		UPDATE notice SET BOARD_TITLE = #{boardTitle}, BOARD_WRITER = #{boardWriter}, BOARD_CONTENT = #{boardContent}
		WHERE BOARD_NUM = #{boardNum}
	</update>
	
	<delete id="delete" parameterType="NoticeVO">
		DELETE FROM notice WHERE BOARD_NUM = #{boardNum}
	</delete>
	
	<delete id="fileDelete" parameterType="NoticeVO">
		DELETE FROM notice_files WHERE BOARD_NUM = #{boardNum}
	</delete>
	
	<select id="detail" parameterType="NoticeVO" resultMap="detailResultMap">
		SELECT * FROM notice n LEFT JOIN notice_files nf ON n.BOARD_NUM = nf.BOARD_NUM WHERE n.BOARD_NUM = #{boardNum};
	</select>
	
	<select id="noticeList" resultType="NoticeVO" parameterType="Pager">
		<if test="startIndex >= 0">
			SELECT * FROM notice <include refid="search"></include> ORDER BY BOARD_NUM DESC
			LIMIT #{startIndex}, #{endIndex}		
		</if>
		<if test="0 > startIndex">
			SELECT * FROM notice
		</if>
	</select>
	
	<select id="getTotalCount" resultType="Long" parameterType="Pager">
		SELECT COUNT(BOARD_NUM) FROM notice <include refid="search"></include>
	</select>
	
	<insert id="insertFile" parameterType="BoardFileVO">
		INSERT INTO notice_files VALUES (NULL, #{boardNum}, #{oriName}, #{saveName})
	</insert>
	
	<select id="fileDetail" parameterType="BoardFileVO" resultType="BoardFileVO">
		SELECT * FROM notice_files WHERE FILE_NUM = #{fileNum}
	</select>
	
	<delete id="fileDeleteByFileNum" parameterType="BoardFileVO">
		DELETE FROM notice_files WHERE FILE_NUM = #{fileNum}
	</delete>
	
</mapper>